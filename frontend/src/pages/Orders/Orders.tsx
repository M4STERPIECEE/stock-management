import Sidebar from '../../components/navigation/sidebar';

const Orders = () => {
    return (
        <Sidebar>
            <div>
                <h1>Orders</h1>
            </div>
        </Sidebar>
    );
};

export default Orders;
