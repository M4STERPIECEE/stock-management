import Sidebar from '../../components/navigation/sidebar';

const Products = () => {
    return (
        <Sidebar>
            <div>
                <h1>Products</h1>
            </div>
        </Sidebar>
    );
};

export default Products;
